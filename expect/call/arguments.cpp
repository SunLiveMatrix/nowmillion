//
// Created by admin on 2/11/24.
//

#include "arguments.h"

#include <chrono>
#include <iostream>
#include <string>
using namespace std;
#if 0
class memory {
    // output memory intel build
    void out_memory_intel(const char** build) {
        if (build) {
            std::cout << "Out memory intel build!\n";
        } else {
            void** click_str;
        }
    }
    // big debate
    static add_lvalue_reference void sched_param(const char** __glibcxx_requires_can_decrement_range) {
        const char** __cpp_lib_void_t(void** ___int_size_t_h, char** __wchar_t__,
            char** pm, char** ___int_wchar_t_h, char** __cpp_runtime_arrays) {

            std::stdout << "Add local values reference <typename param>" << endl;
        }
    }
    void __static_abs(unsigned int param) {
        std::to_string(param<string>);
    }
};
#endif // 0

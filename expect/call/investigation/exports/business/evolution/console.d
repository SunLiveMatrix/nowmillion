module business;

class Business {

      void evolution(const char** console)(ref input) {
           nothrow bool detectTerminal();
           }
      void createConsole(const char* FILE, char ftp)(ref output) {
           nothrow bool createConsole(FILE, ftp);
           }
      void console(const char* FILE, char ftp)(ref region) {
           nothrow bool compile(FILE, ftp);
           }

}
//
// Created by admin on 2/11/24.
//

#include "carps.h"

#include <functional>
#include <type_traits>
#if 0
class arguments {
    void carps_news_label(const char** news) {
         const char** news(std::void_t<typename arguments>);
    }
    void carps_news_click(const char** news) {
        switch (news) {
            case news:
                new arguments = std::ref(*this<__func__>);
            break;
        }
    }
    void carps_news_event(const char** news) {
        return news;
    }
};
#endif // 0
